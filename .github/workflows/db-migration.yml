name: DB Migration

on:
  workflow_dispatch: {}

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install
        run: pnpm install
      - name: Run migrations
        run: pnpm --filter "@hq-operations/database" db:migrate
